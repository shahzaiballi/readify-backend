Django==5.1
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.4.0
psycopg2-binary==2.9.9
python-decouple==3.8

# PDF processing
PyMuPDF==1.24.5

# AI — DeepSeek V3 (primary) via OpenAI-compatible API
# Replaces anthropic — DeepSeek is ~20x cheaper, same quality
openai>=1.30.0

# AI — Google Gemini (FREE tier, used only for text embeddings in RAG)
google-generativeai>=0.7.0

# Background tasks (Celery + Beat scheduler)
celery==5.4.0
redis==5.0.7
django-celery-results==2.5.1
django-celery-beat==2.6.0

# File uploads
Pillow==10.4.0

# Google OAuth
google-auth==2.32.0

# Email
# (uses Django's built-in SMTP backend)